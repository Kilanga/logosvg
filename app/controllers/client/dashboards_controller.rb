module Client
  # Client dashboard. Step 5 fills it with the actions awaiting the client and
  # the recent activity; for now it only proves the space is theirs alone.
  class DashboardsController < BaseController
    def show
    end
  end
end
